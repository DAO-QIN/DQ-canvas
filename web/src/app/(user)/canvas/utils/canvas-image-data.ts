export {
    MAX_WORKSPACE_UPSCALE_LONG_EDGE as MAX_UPSCALE_LONG_EDGE,
    cropWorkspaceImage as cropDataUrl,
    resolveWorkspaceUpscaleSize as resolveUpscaleSize,
    splitWorkspaceImage as splitDataUrl,
    upscaleWorkspaceImage as upscaleDataUrl,
} from "@/components/creative-workspace/workspace-image-data";
export type { WorkspaceImageSplitParams as ImageSplitParams, WorkspaceImageUpscaleAlgorithm as ImageUpscaleAlgorithm, WorkspaceImageUpscaleParams as ImageUpscaleParams } from "@/components/creative-workspace/workspace-image-data";
